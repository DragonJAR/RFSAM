export default {
  "status": "complete",
  "facts": [
    { "k": "Band", "v": "Sub-GHz ISM, region-locked: 908.42 MHz (US) · 868.42 MHz (EU) · plus regional channels (e.g. 921.42 MHz ANZ, 919.82 MHz HK, 922–926 MHz JP). One region per device — pick the right frequency first." },
    { "k": "Modulation / rate", "v": "(G)FSK at three data rates: 9.6 kbps (R1, legacy), 40 kbps (R2) and 100 kbps (R3). Z-Wave Long Range (US 912/920 MHz) adds higher power and star topology." },
    { "k": "PHY / MAC", "v": "ITU-T G.9959 — the open PHY/MAC standard underneath Z-Wave (preamble, frame, CRC, addressing). The proprietary upper stack opened up after Silicon Labs released it (~2016+)." },
    { "k": "Identifiers", "v": "32-bit Home ID (the network, fixed by the primary controller) + 8-bit Node ID (the device, 1–232). Both are visible in the clear in every frame header — the network fingerprint." },
    { "k": "Security", "v": "Legacy S0: AES-128, but during device inclusion the network key is sent encrypted under a FIXED, well-known temporary key (all-zero) — capture the inclusion and you recover the network key. Modern S2 (Gen5/700+): Curve25519 ECDH key exchange at inclusion — the shared secret is never sent, so the S0 capture-the-join break does not apply." },
    { "k": "Topology / targets", "v": "Source-routed mesh: a primary controller/hub plus routing slaves and end devices. Common targets: door locks, garage/window sensors, thermostats and the controller/hub itself." }
  ],
  "reference": null,
  "layers": {
    "IG": {
      "note": "Work out what the device is before any RF work — no capture yet. Z-Wave's first fork is the region: a US device sits at 908.42 MHz and an EU device at 868.42 MHz (with several other regional channels), and a radio tuned to the wrong one hears nothing — so confirm the frequency before anything else. Then the security generation decides whether the rest of this descent even applies: a legacy S0 device has a known inclusion weakness, while a modern S2 device (Z-Wave Gen5/700 and later) uses Curve25519 ECDH and resists the classic break. The Home ID and Node IDs are sent in the clear in every frame, so once you can hear the network you can fingerprint it.",
      "lookFor": [
        "Region / frequency — US 908.42 MHz vs EU 868.42 MHz vs other regional channels (ANZ 921.42, HK 919.82, JP 922–926…). Read the FCC ID / CE marking on the label; it sets which radio can even tune the device.",
        "Chipset / SoC and generation — Sigma Designs ZW0301/ZW0501 (3rd/5th gen, 500-series) vs Silicon Labs 700/800-series (EFR32ZG). FCC ID and teardown; the generation hints at S0-only vs S2 support. Cross-reference Z-Wave advisories (the S0 inclusion key exposure; Z-Shave downgrade).",
        "Security class in use — S0 (legacy AES-128, vulnerable inclusion key transport) vs S2 (Curve25519 ECDH, much stronger) vs unencrypted command classes. A controller can also downgrade S2→S0 (Z-Shave) if the device still offers S0.",
        "Home ID (32-bit, the network) and Node ID (8-bit, the device) — both sent in the clear in every frame header; the network's fingerprint and your filter once you can hear it.",
        "Device type and exposed command classes — door lock, sensor, switch, thermostat, controller/hub — and which command classes it answers (some are sent unencrypted even on a secure network).",
        "Data rate it speaks — 9.6 / 40 / 100 kbps (R1/R2/R3), and whether it is classic Z-Wave or Z-Wave Long Range — sets the demodulator parameters at capture."
      ]
    },
    "SP": {
      "note": "Confirm the device is transmitting and on which regional channel, before committing a decoder. Z-Wave lives in a narrow sub-GHz ISM slice — one region per device — so 'see it' means tuning a waterfall to the regional centre (908.42 MHz US / 868.42 MHz EU) and watching for the short FSK bursts the device emits when it reports or is polled. A cheap RTL-SDR reaches these sub-GHz bands fine (unlike 2.4 GHz protocols), so this is the one band where the budget dongle is a first-class survey radio; a HackRF gives more bandwidth to watch several channels/rates at once. Z-Wave is bursty and event-driven — trigger the device (open the door, trip the sensor) to make it talk while you watch.",
      "tools": [
        { "tool": "gqrx", "role": "Spectrum / waterfall view", "why": "Tune to the regional centre (908.42 MHz US / 868.42 MHz EU) and watch for the short FSK bursts a Z-Wave device emits when it reports or is polled — confirm the device is alive and on the channel you expect before committing a decoder. A cheap RTL-SDR reaches these sub-GHz bands; a HackRF shows more of the band at once.", "deps": ["rtl-sdr-v4", "hackrf-one"] }
      ]
    },
    "PHY": {
      "note": "No standalone Z-Wave demodulator separate from capture in this kit. The G.9959 (G)FSK waveform is demodulated (PHY) and the frame parsed (LL) together inside each capture tool, so PHY and Capture are one step. The job here is to match the demodulator to the device: pick the regional frequency (908.42/868.42 MHz) and the data rate (9.6/40/100 kbps) — Z-Wave's three rates use different deviation/bandwidth, so a decoder set for 100 kbps will miss a 9.6 kbps legacy frame. The SDR path (Waving-Z / rtl-zwave / EZ-Wave) channelises the raw I/Q in software; the vendor Zniffer does demod and framing in the stick's own radio."
    },
    "LL": {
      "note": "Capture and decode Z-Wave frames. Park a radio on the regional channel and pull the frame headers (Home ID, Node ID, frame control, command class) off the air. Two paths: the SDR path demodulates the G.9959 waveform in software — Waving-Z or rtl-zwave on a cheap RTL-SDR for a quick read, or EZ-Wave / Scapy-radio on a HackRF for a scriptable assessment stack — while the vendor path uses Silicon Labs' Zniffer GUI on a UZB stick for a turnkey, fully-dissected capture. CRITICAL for the crypto step: to break an S0 network you must capture a device inclusion (pairing) — power-cycle or re-pair the target while capturing so the inclusion key transport hits the air.",
      "decoder": "wireshark",
      "tools": [
        { "tool": "waving-z", "role": "Primary SDR decoder", "why": "An ITU-T G.9959 (de)modulator: pipe `rtl_sdr` (or HackRF) I/Q into `wave-in` and it prints decoded Z-Wave frames — the rtl_433-style entry point for getting packets off a ~$30 dongle. Set the regional frequency and data rate to match the device.", "caveat": "Receive-side decoding to stdout; not a full dissector — pair with the Wireshark dissector or EZ-Wave for command-class detail.", "deps": ["rtl-sdr-v4", "hackrf-one"] },
        { "tool": "rtl-zwave", "role": "Minimal RTL-SDR decoder", "why": "The original G.9959 demodulator Waving-Z grew from: pipe `rtl_sdr` samples in and read decoded frames out. The smallest way to confirm Z-Wave traffic and read Home ID / Node ID headers on a cheap RTL-SDR.", "caveat": "Receive-only, RTL-SDR only; minimal output, older codebase.", "deps": ["rtl-sdr-v4"] },
        { "tool": "ez-wave", "role": "Assessment-suite capture", "why": "ezstumbler does passive discovery and active enumeration over a HackRF (GNU Radio + Scapy-radio), giving a scriptable capture you carry into recon and attack — the canonical Z-Wave assessment toolkit. Default config drives two HackRF One SDRs.", "caveat": "Python 2.7 / GNU Radio 3.7 era; setup is dated and fiddly. The same SDR/Scapy plumbing underlies the attack and app steps.", "deps": ["hackrf-one"] },
        { "tool": "scapy-radio", "role": "Scriptable SDR sniff/craft", "why": "GNU Radio flowgraphs plus a patched Scapy with a Z-Wave layer: sniff, dissect and replay Z-Wave frames straight from a Python session over a HackRF. The radio/Scapy plumbing EZ-Wave builds on, usable standalone to script capture and injection.", "caveat": "Older GNU Radio codebase; the Z-Wave layer is one of several protocols it carries.", "deps": ["hackrf-one"] },
        { "tool": "zwave-zniffer", "role": "Vendor sniffer (turnkey)", "why": "Silicon Labs' official Zniffer GUI (in Simplicity Studio) captures and fully dissects Z-Wave RF — command classes, security handshakes, the inclusion exchange — on a UZB stick flashed with Zniffer firmware. The easiest, most accurate capture when an SDR demod is too fiddly, and the cleanest way to watch an S0 inclusion.", "caveat": "Needs a Silicon Labs UZB stick re-flashed with Zniffer firmware; vendor GUI, not an open-source SDR path.", "deps": ["silabs-uzb7"] }
      ]
    },
    "CR": {
      "note": "Be honest about what is breakable. Z-Wave has two security worlds. LEGACY S0 (AES-128) has a real, well-known weakness: during device inclusion the controller sends the network key encrypted under a FIXED, all-zero temporary key — so anyone who captured that inclusion exchange can decrypt the key transport and recover the network key, then decrypt the rest of the S0 traffic. This is exactly why the Capture step insists on grabbing an inclusion: no inclusion capture, no S0 key. MODERN S2 (Z-Wave Gen5/700+) closes this — inclusion uses a Curve25519 ECDH key exchange, the shared secret is never transmitted, and there is no equivalent capture-the-join shortcut; an S2 recording yields no key offline. (The historical Z-Shave attack instead downgrades an S2-capable device back to S0 during inclusion — a downgrade, not a break of S2's crypto.) So: against S0, capture the inclusion and extract the key; against S2, recognise it and stop.",
      "tools": [
        { "tool": "zwave-zniffer", "role": "S0 inclusion capture / key transport", "why": "The cleanest way to capture and read the S0 inclusion exchange — the moment the network key is sent encrypted under the fixed all-zero temporary key. The Zniffer dissects the frames so you can see the key-transport command and, knowing the fixed temporary key, recover the S0 network key; load that key back into the Zniffer (or your decoder) to decrypt the rest of the S0 traffic.", "caveat": "Only the legacy S0 inclusion is exposed this way — S2's ECDH inclusion reveals no key. Needs the UZB stick + Zniffer firmware and a captured (re-)inclusion.", "deps": ["silabs-uzb7"], "needs": "A capture that contains a device INCLUSION on an S0 network — captured at the Capture step (Zniffer, or an SDR decoder watching the join). To force one, exclude and re-include (re-pair) the target while capturing. No inclusion capture means no S0 key." },
        { "tool": "ez-wave", "role": "S0 traffic decryption (with key)", "why": "Once you hold the S0 network key (recovered from the inclusion's fixed-temporary-key transport), EZ-Wave / Scapy-radio can decrypt and dissect the S0 frames it captured over the HackRF — turning ciphertext into readable command classes for recon and attack.", "caveat": "S0 only; against S2 (Curve25519 ECDH) there is no key to recover and nothing to decrypt. Dated Python 2.7 / GNU Radio stack.", "deps": ["hackrf-one"], "needs": "The S0 network key derived from the captured inclusion (the key transport is encrypted under the known fixed all-zero temporary key), plus the S0 capture from the Capture step." }
      ]
    },
    "AT": {
      "note": "Going active means transmitting in licensed-exempt sub-GHz ISM and injecting into someone's home-automation network — authorised testing only, on devices you own or have permission to assess. What is possible depends entirely on the crypto from the previous step. On a legacy S0 network where you recovered the key (or against UNENCRYPTED command classes, which exist even on secure networks), you can forge and inject command-class frames — replay a captured command, or craft a new one (e.g. a lock/unlock, switch on/off) and transmit it. The TX path is the SDR side of the same tools: EZ-Wave / Scapy-radio over a HackRF, or gr-zwave_poore as a GNU Radio TX block on a USRP B210. Against a properly-configured S2 network you cannot forge valid encrypted frames without the key, and S2 adds nonce-based anti-replay — so injection there is limited to whatever the device still exposes unencrypted.",
      "tools": [
        { "tool": "ez-wave", "role": "Inject / forge command classes", "why": "With the S0 key (or against unencrypted command classes), EZ-Wave's Scapy-radio stack crafts and transmits Z-Wave command-class frames over a HackRF — replay a captured command or forge a new one (switch, lock, sensor report) into the target network.", "caveat": "Effective against S0 (with the key) and unencrypted classes; S2's ECDH + anti-replay nonces block forged/replayed encrypted frames. Authorised testing only.", "deps": ["hackrf-one"], "needs": "For encrypted S0 targets, the network key from the Break-crypto step; for unencrypted command classes, just a capture to model the frame. Plus a TX-capable HackRF on the device's regional frequency." },
        { "tool": "scapy-radio", "role": "Scriptable replay / injection", "why": "Build and transmit arbitrary Z-Wave frames from a Scapy session over a HackRF — the flexible path to replay a captured frame wholesale or fuzz/forge command classes when you want full control of the bytes.", "caveat": "Same S0-vs-S2 limits as above; older GNU Radio codebase. Authorised testing only.", "deps": ["hackrf-one"], "needs": "A captured frame to replay, or the S0 key to forge valid encrypted frames, plus a TX-capable HackRF on the regional frequency." },
        { "tool": "gr-zwave_poore", "role": "GNU Radio Z-Wave TX block", "why": "A maintained GNU Radio out-of-tree module that transmits Z-Wave signals (tested on a USRP B210, branches for GNU Radio 3.7/3.8/3.10, integrated into FISSURE) — the flowgraph path to generate/replay Z-Wave frames when you prefer a GNU Radio block to the Scapy-radio stack.", "caveat": "Primarily a transmitter; pair with an SDR decoder for RX. Authorised testing only.", "deps": ["usrp-b210"], "needs": "The frame/payload to transmit (a captured command, or a forged one using the S0 key from the Break-crypto step) and a TX-capable USRP B210 on the regional frequency." }
      ]
    },
    "AP": {
      "note": "Above the link sits the application — the Z-Wave command classes the device exposes: lock/unlock (Door Lock CC), on/off (Binary Switch CC), thermostat setpoints, sensor reports, configuration parameters, and the Version/Manufacturer-Specific classes that fingerprint it. Two ways to drive them. Read them out of capture: EZ-Wave's ezrecon interrogates a device for its manufacturer/model, firmware version, supported command classes and configuration (and ezfingerprint identifies the module generation). Or drive them legitimately from a controller: Silicon Labs' Z-Wave PC Controller, over a UZB stick, includes/excludes nodes and sends command-class messages to the device — the supported, vendor way to enumerate and exercise what the device trusts above the link (and to set up controlled S0/S2 inclusions to study at the lower steps).",
      "tools": [
        { "tool": "ez-wave", "role": "Device recon / command-class enumeration", "why": "ezrecon interrogates a Z-Wave device — manufacturer and model, software/firmware versions, supported command classes and configuration settings — and ezfingerprint identifies the module generation via a PHY preamble-length trick. The fastest read of what application surface the device exposes, over a HackRF.", "caveat": "Active interrogation transmits to the device; dated Python 2.7 / GNU Radio stack. Authorised testing only.", "deps": ["hackrf-one"] },
        { "tool": "zwave-pc-controller", "role": "Controller / command-class driver", "why": "Silicon Labs' official PC Controller drives a real Z-Wave controller node over a UZB stick's Serial API: include/exclude nodes, browse the device's supported command classes and send them — the supported way to enumerate and exercise the application layer, and to stage controlled S0/S2 inclusions you then study at the capture/crypto steps.", "caveat": "Vendor GUI; needs a Silicon Labs UZB stick. Operates as a legitimate controller rather than a covert injector.", "deps": ["silabs-uzb7"], "needs": "A Silicon Labs UZB stick running the Serial API (controller firmware). To exercise a target you must be able to include it into your test network (or already be its controller)." }
      ]
    }
  }
};
